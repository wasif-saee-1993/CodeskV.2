@model WasifProject.Models.Admin
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

<section id="loginForm">

@using (Html.BeginForm("ValidateAndLogin","Admin","post"))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <div >
  <table >
            <tr>
              <td>  @Html.LabelFor(m => m.userName)</td>
               <td> @Html.TextBoxFor(m => m.userName)</td>
               <td> @Html.ValidationMessageFor(m => m.userName)</td>
            </tr>
            <tr>
               <td> @Html.LabelFor(m => m.C_passWord)</td>
               <td> @Html.PasswordFor(m => m.C_passWord)</td>
               <td>  @Html.ValidationMessageFor(m => m.C_passWord)</td>
            </tr>
           <tr> <td colspan="3" ><input type="submit" value="Login"  /></td></tr>
      
        </table>
        </div>
    <div id="error" style="font-size:16px;color:red;font-weight:bold;">
                    @ViewBag.ErrorMassge
    </div>
    
}
</section>
@section scripts {
@Scripts.Render("~/bundles/jquery")
<script>
    $(document).ready(function () {
      function  loginVerify(){
            if ($("#userName").val == "") {
                alert('User Name =');
                $("error").html = "please enter username";
                return false;
            }
            if ($("#passwork").val == "") {
                $("error").html = "please enter Password";
                return false;
            }
            return true;
        }
    });
</script>
}